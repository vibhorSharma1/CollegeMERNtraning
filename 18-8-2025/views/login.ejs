<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>3D Login Form</title>
    <style>
      .perspective {
        perspective: 1000px;
      }
      .form-3d:hover {
        transform: rotateY(10deg) scale(1.03);
        box-shadow: 0 10px 40px rgba(0,0,0,0.2);
      }
      .form-3d {
        transition: transform 0.3s, box-shadow 0.3s;
      }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-900 to-purple-900 min-h-screen flex items-center justify-center">
  <div class="perspective">
    <form class="form-3d bg-white bg-opacity-90 rounded-xl shadow-2xl p-8 w-80 flex flex-col gap-6" action="/user/loginUser" method="POST">
      <h2 class="text-2xl font-bold text-center text-purple-700 mb-4">Login</h2>
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-1" for="email">Email</label>
        <input class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-purple-500 transition" type="email" id="email" name="email" required />
      </div>
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-1" for="password">Password</label>
        <input class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-purple-500 transition" type="password" id="password" name="password" required />
      </div>
      <button type="submit" class="bg-purple-700 text-white font-semibold py-2 rounded-lg shadow-lg hover:bg-purple-800 transition transform hover:scale-105">Sign In</button>
        <p class="text-sm text-gray-600 text-center mt-4">Don't have an account? <a href="/user/register" class="text-purple-600 hover:underline">Register here</a></p>
    </form>
  </div>
</body>
</html>