<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Edit Student</title>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
  <div class="bg-white p-8 rounded shadow-lg w-full max-w-lg">
    <h2 class="text-2xl font-bold mb-6 text-center">Edit Student</h2>
    <form action="/student/update/<%= student._id %>" method="POST" class="space-y-4">

      <input type="text" name="rollNo" value="<%= student.rollNo %>" 
             class="w-full border px-4 py-2 rounded" placeholder="Roll No" required>

      <input type="text" name="studentName" value="<%= student.studentName %>" 
             class="w-full border px-4 py-2 rounded" placeholder="Student Name" required>

      <input type="text" name="fatherName" value="<%= student.fatherName %>" 
             class="w-full border px-4 py-2 rounded" placeholder="Father Name" required>
       
       <input type="text" name="motherName" value="<%= student.motherName %>" 
             class="w-full border px-4 py-2 rounded" placeholder="Mother Name" required>      

      <input type="text" name="course" value="<%= student.course %>" 
             class="w-full border px-4 py-2 rounded" placeholder="Course" required>

      <input type="text" name="branch" value="<%= student.branch %>" 
             class="w-full border px-4 py-2 rounded" placeholder="Branch" required>

      <input type="number" name="yearOfAdmission" value="<%= student.yearOfAdmission %>" 
             class="w-full border px-4 py-2 rounded" placeholder="Year of Admission" required>

      <div class="flex justify-between">
        <a href="/student/listStudentbb" 
           class="px-4 py-2 bg-gray-500 text-white rounded hover:bg-gray-600">Cancel</a>
        <button type="submit" 
                class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">Update</button>
      </div>
    </form>
  </div>
</body>
</html>
